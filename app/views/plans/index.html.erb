<% days = (@start_date..@end_date).to_a %>
<div class="space-y-6" data-controller="meal-selector">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-xl font-bold text-slate-900">献立カレンダー</h1>
      <p class="text-sm text-[var(--muted)] mt-1">今週の献立をまとめて管理し、買い物リストを自動計算します。</p>
    </div>
  </div>

  <div class="soft-card p-4">
    <div class="calendar-scroll flex gap-2 overflow-x-auto pb-2">
      <% days.each do |date| %>
        <% active = date == @selected_date %>
        <% is_today = date == Date.current %>
        <%= link_to plans_path(date: date), class: "calendar-pill #{'calendar-pill--active' if active} #{'calendar-pill--today' if is_today}" do %>
          <span class="text-[11px] font-semibold <%= active ? 'text-white' : 'text-slate-500' %>"><%= is_today ? '今日' : weekday_label(date) %></span>
          <span class="text-xl font-bold <%= active ? 'text-white' : 'text-slate-800' %>"><%= date.day %></span>
        <% end %>
      <% end %>
    </div>
  </div>

  <div class="space-y-4">
    <div class="flex items-center gap-2 px-1">
      <div class="w-1.5 h-6 rounded-full bg-slate-900"></div>
      <h2 class="text-lg font-bold text-slate-900"><%= l @selected_date, format: :long %>（<%= weekday_label(@selected_date) %>）の献立</h2>
    </div>

    <% MealPlan.meal_types.keys.each do |meal_type| %>
      <% slot_plans = @plans_by_slot[[@selected_date, meal_type]] || [] %>
      <% badge_class = case meal_type
        when "morning" then "badge-gray"
        when "lunch" then "badge-orange"
        else "badge-purple"
      end %>
      <% placeholder = case meal_type
        when "morning" then "朝食を追加"
        when "lunch" then "昼食を追加"
        else "夕食を追加"
      end %>
      <% empty_slot = slot_plans.empty? %>
      <div class="slot-card <%= 'slot-card--clickable' if empty_slot %>"
           <% if empty_slot %>
             data-action="click->meal-selector#open"
             data-meal-selector-plan-date-value="<%= @selected_date %>"
             data-meal-selector-meal-type-value="<%= meal_type %>"
           <% end %>>
        <span class="slot-badge <%= badge_class %>"><%= meal_type.upcase %></span>

        <% if slot_plans.any? %>
          <div class="space-y-3">
            <% slot_plans.each do |plan| %>
              <div class="flex items-center gap-4">
                <div class="slot-thumb">
                  <% if plan.recipe.photo.attached? %>
                    <%= image_tag plan.recipe.photo.variant(resize_to_fill: [72, 72]), class: "w-full h-full object-cover rounded-full" %>
                  <% else %>
                    <span class="text-2xl">🍽️</span>
                  <% end %>
                </div>
                <div class="flex-1">
                  <p class="font-semibold text-slate-900"><%= plan.recipe.name %></p>
                  <p class="text-sm text-[var(--muted)] mt-1"><span class="text-[var(--brand)] font-bold"><%= plan.servings %></span> 人分</p>
                  <div class="flex items-center gap-2 mt-2">
                    <%= form_with(model: plan, url: meal_plan_path(plan), method: :patch, class: "flex items-center gap-2") do |f| %>
                      <%= f.number_field :servings, step: "0.5", min: "0.5", class: "w-20 text-sm text-center rounded-lg border-slate-200 focus:ring-[var(--brand)] focus:border-[var(--brand)]" %>
                      <%= f.submit "更新", class: "btn-tiny bg-[var(--brand)] text-white hover:bg-[var(--brand-dark)] shadow-[0_8px_20px_rgba(255,122,0,0.25)]" %>
                    <% end %>
                    <%= form_with url: meal_plan_path(plan), method: :delete, class: "inline", local: true do %>
                      <%= button_tag "削除",
                        type: "button",
                        data: {
                          action: "confirm-modal#ask",
                          confirm_modal_message_value: "この献立を削除しますか？"
                        },
                        class: "btn-tiny btn-danger" %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
          <div class="flex justify-end">
            <button
              type="button"
              class="slot-add-button"
              data-action="click->meal-selector#open"
              data-meal-selector-plan-date-value="<%= @selected_date %>"
              data-meal-selector-meal-type-value="<%= meal_type %>">
              この枠にレシピを追加
            </button>
          </div>
        <% else %>
          <div class="slot-empty w-full text-left">
            <div class="slot-empty__icon">＋</div>
            <p class="slot-empty__text"><%= placeholder %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="shopping-card soft-card">
    <div class="shopping-card__header">
      <div class="flex items-center gap-2">
        <span class="text-indigo-500 text-xl">🛒</span>
        <div>
          <p class="font-semibold text-indigo-900">買い物リスト</p>
          <p class="text-xs text-indigo-500 mt-0.5">表示中の週間献立から自動計算</p>
        </div>
      </div>
      <%= button_tag "クリップボードにコピー",
        type: "button",
        id: "copy-shopping-list",
        data: { shopping_text: @shopping_list.map { |item| "#{item.name} #{format_quantity(item.quantity)}#{item.unit}" }.join("\n") },
        disabled: @shopping_list.empty?,
        class: "btn-ghost text-indigo-600 border-indigo-100 hover:bg-indigo-50 disabled:opacity-50 disabled:cursor-not-allowed" %>
    </div>

    <% if @shopping_list.any? %>
      <div class="shopping-list">
        <% @shopping_list.each do |item| %>
          <div class="shopping-row">
            <div class="flex items-center gap-3">
              <div class="shopping-dot"></div>
              <span class="font-medium text-slate-800"><%= item.name %></span>
            </div>
            <span class="font-semibold text-indigo-900"><%= format_quantity(item.quantity) %><span class="text-sm text-slate-500 ml-1"><%= item.unit %></span></span>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="shopping-empty">
        <div class="shopping-empty__icon">👜</div>
        <p class="shopping-empty__text">献立を追加してリストを作成</p>
        <p class="shopping-empty__hint">表示中の期間の献立をもとに自動で集計されます</p>
      </div>
    <% end %>
  </div>

  <%= render "plans/selector_modal", recipes: @recipes %>

  <script>
    (() => {
      const attachCopyHandler = () => {
        const btn = document.getElementById("copy-shopping-list");
        if (!btn || btn.dataset.bound === "true") return;

        btn.addEventListener("click", async () => {
          const text = btn.dataset.shoppingText || "";
          if (!text.trim()) return;
          try {
            await navigator.clipboard.writeText(text);
            const original = btn.textContent;
            btn.textContent = "コピーしました";
            setTimeout(() => (btn.textContent = original), 1200);
          } catch (e) {
            btn.textContent = "コピー失敗";
            setTimeout(() => (btn.textContent = "クリップボードにコピー"), 1200);
          }
        });

        btn.dataset.bound = "true";
      };

      document.addEventListener("turbo:load", attachCopyHandler);
      document.addEventListener("turbo:render", attachCopyHandler);
    })();
  </script>
</div>
