<!DOCTYPE html>
<html>
  <head>
    <title>JisuicookShoppingList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[var(--surface-2)] min-h-screen" data-controller="confirm-modal">
    <header class="brand-bar">
      <div class="flex items-center space-x-3">
        <div class="w-9 h-9 rounded-xl bg-white/20 flex items-center justify-center text-2xl">🛍️</div>
        <div class="flex flex-col leading-tight">
          <span class="text-lg font-semibold">Chef's Note</span>
          <span class="text-xs opacity-90">献立と買い物をスマートに</span>
        </div>
      </div>
    </header>

    <div class="max-w-5xl mx-auto px-4 pb-24 pt-5">
      <% flash.each do |key, value| %>
        <div class="mb-4 px-4 py-3 rounded-xl border shadow-sm <%= key.to_s == 'notice' ? 'bg-green-50 border-green-200 text-green-800' : 'bg-yellow-50 border-yellow-200 text-yellow-800' %>">
          <%= value %>
        </div>
      <% end %>

      <main>
        <%= yield %>
      </main>
    </div>

    <% current_tab = if controller_name.in?(%w[recipes ingredients])
        :recipes
      else
        :plans
      end %>

    <nav class="bottom-nav">
      <%= link_to recipes_path, class: "bottom-nav__item #{current_tab == :recipes ? 'bottom-nav__item--active' : ''}" do %>
        <span class="text-xl"><%= current_tab == :recipes ? "🧾" : "🧾" %></span>
        <span class="mt-1">レシピ</span>
      <% end %>
      <%= link_to plans_path, class: "bottom-nav__item #{current_tab == :plans ? 'bottom-nav__item--active' : ''}" do %>
        <span class="text-xl"><%= current_tab == :plans ? "📅" : "📅" %></span>
        <span class="mt-1">献立・買物</span>
      <% end %>
    </nav>

    <div data-confirm-modal-target="backdrop" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-center justify-center px-4">
      <div class="w-full max-w-sm bg-white rounded-2xl shadow-2xl p-6 space-y-4">
        <div class="text-lg font-bold text-slate-900" data-confirm-modal-target="message">本当に削除しますか？</div>
        <div class="flex gap-3">
          <button class="flex-1 py-2 rounded-xl border border-slate-200 text-slate-600 font-semibold hover:bg-slate-50" data-action="confirm-modal#cancel">キャンセル</button>
          <button class="flex-1 py-2 rounded-xl bg-red-500 text-white font-semibold shadow hover:bg-red-600" data-action="confirm-modal#confirm">削除する</button>
        </div>
      </div>
    </div>
  </body>
</html>
